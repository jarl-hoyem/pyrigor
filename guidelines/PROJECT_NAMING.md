# Project naming convention

The project name is always written **pyrigor**, in lowercase.

Use `pyrigor` in documentation, prose, headings, issue titles, and other project references. Do not capitalise it as `Pyrigor` or `PyRigor`.
